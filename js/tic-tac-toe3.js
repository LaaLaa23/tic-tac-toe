$(document).ready(function() {
  console.log('hello');

/*
// OVERWRITE / NO DRAW / UNTIL WIN!

// Selecting Player:
  var playerTurn=0;

  $('.gameboard').find('td').on('click', function() {
    if (playerTurn % 2 === 0) {
      $(this).text('X');
      checkWin('X');
    } else {
    $(this).text('O'); // player 2's turn (O)
    checkWin('O');
    }
    playerTurn++;
    });

    function checkWin(player){
      if ($('#board').find('#1').text() !== '') { // checks what is in 1st tile is not blank
        if ($('#board').find('#1').text() == $('#board').find('#2').text()) { // & checks 1st and 2nd tiles same
          if ($('#board').find('#1').text() == $('#3').text()) { // & checks if 3rd is same
            alert('Game over! '+player+' is the winner!'); // WIN
              $('#resetGame').removeClass('invisible'); //




  $('#resetGame').on('click',function(){
    $('.tile').text('');
  });

*/

});
